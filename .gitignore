node_modules/
out/
dist/
release/
.env
.env.local
*.log

# Cache incremental de TypeScript: se regenera en cada compilacion.
*.tsbuildinfo

# Recursos de marca: los dibuja scripts/generar-recursos.mjs en cada empaquetado.
# El script si se versiona; sus imagenes no, para no meter binarios en el repo.
build/*.png
build/*.bmp
build/*.ico
build/*.icns

# El icono si: es el logo de Vilo trazado a mano, no una imagen generada, y sin
# el en el repositorio no se puede empaquetar en un equipo recien clonado.
!build/icon.png

# Originales guardados antes de recortarles el fondo.
.copias/

# Credenciales y tokens del spike de Fase 0 - NUNCA subir esto
.vilo-spike-tokens.json

.DS_Store
Thumbs.db
