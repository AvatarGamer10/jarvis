{
  "name": "jarvis",
  "version": "1.0.0",
  "description": "Asistente escolar de escritorio: calendario, Classroom y organizacion de carpetas",
  "main": "./out/main/index.js",
  "author": "Iker",
  "license": "MIT",
  "private": true,
  "//transformers": "@huggingface/transformers va en devDependencies, no en dependencies: solo se usa en el renderer, que Vite empaqueta. Como dependencia arrastraria onnxruntime-node (210 MB) al instalador sin usarse nunca.",
  "//fuentes": "Las fuentes tambien van en devDependencies: Vite las empaqueta en el bundle del renderer.",
  "//renderer": "lucide-react y thinking-orbs, por lo mismo: solo los usa el renderer. Como dependencias se copiarian ademas al instalador sin que nadie los cargue desde ahi.",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "spike": "node scripts/spike-google.mjs",
    "dev": "electron-vite dev",
    "start": "electron-vite preview",
    "typecheck": "tsc --noEmit -p tsconfig.node.json && tsc --noEmit -p tsconfig.web.json",
    "test": "tsc -p tsconfig.test.json && node --test \"tests/**/*.test.cjs\"",
    "build": "npm run typecheck && electron-vite build",
    "recursos": "node scripts/generar-recursos.mjs",
    "build:win": "npm run recursos && npm run build && electron-builder --win",
    "build:mac": "npm run recursos && npm run build && electron-builder --mac",
    "publicar:win": "npm run recursos && npm run build && electron-builder --win --publish always",
    "publicar:mac": "npm run recursos && npm run build && electron-builder --mac --publish always"
  },
  "dependencies": {
    "electron-updater": "^6.8.9",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@fontsource-variable/archivo": "^5.3.0",
    "@fontsource-variable/inter": "^5.3.0",
    "@fontsource-variable/jetbrains-mono": "^5.3.0",
    "@huggingface/transformers": "^4.2.0",
    "@tailwindcss/vite": "^4.3.3",
    "@types/node": "^22.9.0",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@vitejs/plugin-react": "^4.3.4",
    "electron": "^33.2.0",
    "electron-builder": "^25.1.8",
    "electron-vite": "^2.3.0",
    "lucide-react": "^1.30.0",
    "tailwindcss": "^4.3.3",
    "thinking-orbs": "^0.2.0",
    "typescript": "^5.6.3",
    "vite": "^5.4.11"
  }
}
